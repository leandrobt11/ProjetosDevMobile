import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  titulo:{
    fontSize: 30,
    color: 'green',
    textAlign: 'center',
  },

  tela:{
    marginTop: 20
  },
})

export {styles}